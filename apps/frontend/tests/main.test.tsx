import { describe, expect, it } from 'vitest';

describe('main', () => {
  it('es el punto de entrada principal (no es necesario testear la logica de DOM mount directamente)', () => {
    expect(true).toBe(true);
  });
});
