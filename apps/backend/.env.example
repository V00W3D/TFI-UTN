BUN_MODE=dev
HOSTING_PROFILE=standard

# Opción A — Tailscale sin repetir IP en dos variables:
# HOSTING_PROFILE=local_tailscale
# TAILSCALE_DEVICE_IP=100.x.x.x
# BACKEND_PORT=3000

# Opción B — hosts explícitos:
BACKEND_HOST=0.0.0.0
BACKEND_PORT=3000
FRONTEND_HOST=localhost
FRONTEND_PORT=5173

CORS_EXTRA_ORIGINS=
# REPLIT_DEV_DOMAIN=tu-proyecto.replit.dev

DATABASE_URL=postgresql://user:pass@localhost:5432/qart_db
SESSION_SECRET=change-me
REFRESH_SECRET=change-me
