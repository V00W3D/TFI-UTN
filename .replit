modules = ["nodejs-20", "web", "postgresql-16"]
[agent]
expertMode = true

[nix]
channel = "stable-25_05"

[userenv]

[userenv.shared]
BUN_MODE = "dev"
BACKEND_PORT = "3000"
BACKEND_HOST = "localhost"
FRONTEND_PORT = "5000"
FRONTEND_HOST = "0.0.0.0"
VITE_MODE = "dev"
VITE_BACKEND_HOST = "localhost"
VITE_BACKEND_PORT = "3000"

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Backend API"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Start application"

[[workflows.workflow]]
name = "Backend API"
author = "agent"

[workflows.workflow.metadata]
outputType = "console"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "bun --cwd apps/backend dev"

[[workflows.workflow]]
name = "Start application"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "bun --cwd apps/frontend dev"
waitForPort = 5000

[workflows.workflow.metadata]
outputType = "webview"

[[ports]]
localPort = 3000
externalPort = 80

[[ports]]
localPort = 5000
externalPort = 5000

[deployment]
deploymentTarget = "autoscale"
run = ["bash", "-c", "bun --cwd apps/backend start & bun --cwd apps/frontend preview --host 0.0.0.0 --port 5000"]
build = ["bash", "-c", "bun install && bun --cwd apps/backend run build && bun --cwd apps/frontend run build"]
